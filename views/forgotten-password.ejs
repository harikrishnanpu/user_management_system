<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/login.css">
</head>
<body>
    
    <section class="contianer">

        <div class="login-page-text-container">
                <% if(message){  %>
            <h1>Successfully Changed!</h1>
            <p>Please login to your account with your updated password &#128513</p>
                    <% }else{ %>
            <h1>Forgotten Your Password ?</h1>
            <p>Please remember your next password &#128513</p>
            <% } %>
          </div>
          

        <div class="login-page-form-container">
            <% if(error){  %>
            <p class="error-msg">&#128528; <%= error  %> </p>
            <%  } %>
            <% if(message){  %>
            <p class="success-msg"> <%= message  %> </p>
            <a  class="go-to-btn" href="/login">Go To Login Page</a>
            <%  }else{ %>
            <form action="/user/<%= userId ? userId : '' %>/update/password" method="post" id="login-form">
                
                <div class="form-input">
                    <label for="password">Password</label>
                    <input type="password" name="password" class="<%= error ? 'input-error' : '' %>" id="password" placeholder="enter password" required>
                </div>

                <div class="form-input">
                    <label for="password">Confirm Password</label>
                    <input type="password" name="confirmpassword" class="<%= error ? 'input-error' : '' %>" id="password" placeholder="re-enter password" required>
                </div>

                <div class="form-button-container">
                    <button type="submit">Update</button>
                </div>
            </form>

            <% } %>
            
        </div>

    </section>


</body>
</html>